import { NgModule } from '@angular/core';

// This module is no longer used - the application now uses standalone bootstrap.
// See main.ts for the bootstrap configuration and app.routes.ts for routing.
@NgModule()
export class AppModule { }
