name: Main CI Tests

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_call:

jobs:
  jest-tests:
    uses: TheManOfTeel/SharedWorkflows/.github/workflows/jest-tests.yml@v1.0.0
    with:
      test-command: npm run test:ci
      coverage-path: coverage/
      summary-command: node .github/scripts/jest-summary.js
  cypress-e2e-tests:
    uses: TheManOfTeel/SharedWorkflows/.github/workflows/cypress-e2e-tests.yml@v1.0.0
