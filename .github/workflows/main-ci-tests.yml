name: Main CI Tests

on:
  pull_request:
    branches: ["main"]
  workflow_call:

jobs:
  jest-tests:
    uses: ./.github/workflows/jest-tests.yml
  cypress-e2e-tests:
      uses: ./.github/workflows/cypress-e2e-tests.yml
